const fileTypeGuard = _ref => {
  let {
    uri,
    size,
    name,
    type
  } = _ref;
  if (!uri) return null;
  return {
    uri,
    size: size ?? 0,
    name: name ?? '',
    type: type ?? ''
  };
};

export default fileTypeGuard;
//# sourceMappingURL=fileTypeGuard.js.map