import { useEffect, useState } from 'react';
export const useActiveGroupChannel = (sdk, staleChannel) => {
  const [activeChannel, setActiveChannel] = useState(() => staleChannel);
  useEffect(() => {
    sdk.groupChannel.getChannel(staleChannel.url).then(setActiveChannel);
  }, [staleChannel.url]);
  return {
    activeChannel,
    setActiveChannel
  };
};
//# sourceMappingURL=useActiveGroupChannel.js.map